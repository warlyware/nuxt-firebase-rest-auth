{
  "name": "nuxt-firebase-rest-auth",
  "version": "1.0.0",
  "description": "My impeccable Nuxt.js project",
  "author": "Daniel Ward",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.15.3",
    "firebase": "^5.0.4",
    "js-cookie": "^2.2.0",
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1",
    "nodemon": "^1.11.0"
  }
}
